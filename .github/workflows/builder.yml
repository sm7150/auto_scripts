name: ROM Builder

on: push

jobs: 
  build:
    runs-on: self-hosted

    steps:
       - name: Testting
         run: |
              pwd; ls; mkdir cr10; cd cr10
              repo init -u git://github.com/crdroidandroid/android.git -b 10.0
