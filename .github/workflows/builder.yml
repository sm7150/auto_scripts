name: ROM Builder

on: push

jobs: 
  build:
    runs-on: ice

    steps:
       - name: Echo Test
         run: |
              echo "test"
              
       - name: Notify
         uses: appleboy/telegram-action@master
         with:
           to: ${{ secrets.TEL_CHANNEL }}
           token: ${{ secrets.TEL_TOKEN }}
           message: |
                    The test is test😂
           document: cr10/device/samsung/r1q/device.mk
