name: ROM Builder

on: push

jobs: 
  build:
    runs-on: ice

    steps:
       - name: Testting
         run: |
              git config --list
              pwd; ls; cd cr10
              repo init -u git://github.com/crdroidandroid/android.git -b 10.0
